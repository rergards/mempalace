repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.0
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(mempalace|tests)/.*\.py$
      - id: ruff-format
        files: ^(mempalace|tests)/.*\.py$
